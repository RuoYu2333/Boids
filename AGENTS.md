# Boids 项目开发约束

- 所有项目开发、生成文件和文档写入仅允许发生在 `G:\boids` 内。
- `Reference` 目录保存从 `E:\wmsj` 原样复制的背景资料，默认只读，不在其中维护本项目规则。
- `Reference` 资料里针对旧 ZSF 文档工作区的路径与写入限制不适用于本项目；本文件是 Boids 项目的有效项目级约束。
- 第一阶段只实现二维 Boids，以不同颜色、不同大小的球体代表鱼。
- 模拟采用集中式连续数据与单一 Manager Tick，不为每条代理鱼创建独立 Tick。
- 随机初始化必须支持固定种子，以便复现和测试。
- 核心算法与不直观的边界处理应保留简洁中文注释，并按阶段完成编译和运行验证。
- 不提交 Unreal Engine 生成目录、IDE 缓存、构建产物或本地日志；以仓库根目录 `.gitignore` 为准。

